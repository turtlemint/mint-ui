import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Checkbox from "./index";
import COLORS from "../__utils/colors";

<Meta title="Checkbox" component={Checkbox} parameters={{
componentSubtitle: "default"}} 
/>

# Checkbox

<p style={{ fontSize: "20px", color: COLORS.GREY1}}>all states</p>

<Preview>
	<Story name="default">
		<Checkbox value={false} />
	</Story>
	<Story name="checked">
		<Checkbox value={true} />
		<p>checked</p>
	</Story>
	<Story name="indeterminate">
		<Checkbox value={true} indeterminate={true} />
	</Story>
	<Story name="disabled">
		<Checkbox value={false} disabled />
	</Story>
</Preview>

### Controlled Usage

```
export const CheckboxInAction = () => {
	const [value, setValue] = React.useState<boolean>(false);
	return (
		<Checkbox value={value} onChange={(val: boolean) => setValue(val)} />
	);
};
```